# Local MCP Servers

These lightweight local MCP server modules expose deterministic simulation tools for proof and smoke testing.

They are local-only. Do not expose them publicly. The side-effecting trading-actions server is disabled by default unless `SIMULATION_TRADING_ACTIONS_ENABLED=true` and an approval token is present.
