# ADR 0002: Deterministic Services Own State

Decision: exchange, broker, ledger, risk, and compliance services own state transitions.

Consequence: LLM output cannot directly place orders or mutate the ledger.
